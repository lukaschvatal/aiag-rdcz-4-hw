[project]
name = "assignment-01-gemini-tools"
version = "0.1.0"
description = "Gemini function calling example using the free Open-Meteo API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.21.1",
    "python-dotenv>=1.1.0",
    "requests>=2.32.4",
]
